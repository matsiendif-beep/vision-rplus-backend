[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = [
  "npx prisma generate",
  "./node_modules/.bin/nest build"
]

[start]
cmd = "(npx prisma db seed || echo 'Seed already done') && node dist/src/main"
